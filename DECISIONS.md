# 구현 중 임의 판단 메모

PLAN.md를 기준으로 구현하면서 계획에 없거나 모호해서 임의로 정한 내용입니다. 사내 PC에서 확인한 뒤 바꿔야 하면 대부분 `config.json`만 고치면 됩니다.

## 0. 전제
- **PRD 파일 없음**: PLAN은 `wiki_collelctor_prd.md`도 함께 읽으라고 하지만 저장소와 업로드 파일 어디에도 없었습니다. 그래서 PLAN.md만 보고 구현했습니다. PRD와 다른 부분이 있으면 알려주세요.
- **개발/검증 환경**: Linux 컨테이너(Python 3.12 + Xvfb)에서 개발했습니다. Windows 전용 경로(`CREATE_NO_WINDOW`, `taskkill`, cmd 이스케이프, DPI 설정)와 PyInstaller exe 빌드는 **실행해 보지 않았습니다**.

## 1. CLI 호출 방식
| 판단 | 이유 |
| --- | --- |
| 기본 명령 템플릿을 **stdin 방식**(`claude -p`, `codex exec -`)으로 바꾸고, 프롬프트는 `title_prompt`/`confluence_prompt`/`wiki_prompt` 키로 분리함 | PLAN 예시처럼 `"...\n{text}"`를 명령줄에 넣으면 Windows cmd에서 **줄바꿈이 나오는 지점에서 명령이 끊기고**, 8191자 제한도 걸립니다. PLAN의 "`{text}`가 없으면 stdin" 규칙을 모든 명령으로 넓혔습니다. `{prompt}`나 `{text}`/`{url}`/`{files}`를 cmd에 직접 쓰면 PLAN처럼 인라인 치환도 계속 됩니다. |
| 템플릿 치환은 `str.format`이 아니라 `{이름}`만 바꾸는 정규식을 씀 | 프롬프트에 JSON 예시처럼 중괄호가 들어가도 오류가 나지 않게 하기 위함입니다. |
| 인라인 치환 이스케이프: Windows는 `"`→`\"`와 줄바꿈→공백, POSIX는 `" \ $ \`` 이스케이프 | cmd.exe의 `%` 확장은 처리하지 않았습니다(본문을 인라인으로 넣는 경우에만 문제가 되고, 기본값은 stdin 방식). |
| `codex exec -`가 stdin에서 프롬프트를 읽는다고 가정함 | **확인 필요 (TODO 3)**. 안 되면 `title_cmd`를 `codex exec "{prompt}"`로 바꾸면 됩니다. |
| `wiki_cmd` 기본값에 `--permission-mode acceptEdits`를 추가함 | `claude -p`는 비대화형이라 권한 확인을 할 수 없어서, 이 옵션이 없으면 wiki 파일을 수정하지 못합니다. |
| `confluence_cmd`는 `claude -p`만 둠 | 사내 스킬이 CDP/Bash 등 어떤 도구 권한이 필요한지 몰라서 권한 옵션을 넣지 않았습니다. **TODO 2**에서 `--allowedTools` 등을 확인해야 합니다. |
| 자식 프로세스 환경에 `PYTHONUTF8=1`, `PYTHONIOENCODING=utf-8`을 넣음 | `sgpt` 같은 Python 기반 CLI가 Windows 파이프에 cp949로 출력하면 한글이 깨지기 때문입니다. |
| 입력이 없는 명령은 `stdin=DEVNULL`로 실행 | `--windowed` exe에는 표준입력 핸들이 없어서 자식 프로세스가 오작동할 수 있습니다. |
| timeout이 나면 **프로세스 트리 전체를 종료** (Windows `taskkill /T`, POSIX 프로세스 그룹 kill) | `shell=True`면 cmd.exe만 죽고 그 아래 claude(node)가 남아 파이프를 붙잡기 때문에, 앱이 timeout 뒤에도 계속 멈춰 있게 됩니다. |
| 백그라운드 스레드 결과를 `root.after` polling(100ms)으로 받음 | worker 스레드에서 `root.after`를 직접 호출하면 Tk 빌드에 따라 `RuntimeError: main thread is not in main loop`가 납니다(검증 중에 재현함). |
| 각 명령의 timeout을 config로 분리함: 제목 60초, Confluence 180초, wiki 1800초 | PLAN에는 Confluence 180초만 있었습니다. wiki 변환은 파일 여러 개를 편집하므로 길게 잡았습니다. |

## 2. 메신저 저장
- **파일명 날짜 = 대화 날짜**: 본문에서 처음 나오는 날짜(`date_patterns` 정규식)를 쓰고, 없으면 오늘 날짜를 씁니다. PLAN에서 "대화 날짜"를 wiki 항목에 쓰기로 했기 때문에 파일명과 frontmatter도 같은 날짜로 맞췄습니다.
- frontmatter에 **`date` 필드를 추가**했습니다(PLAN 7절의 "frontmatter의 대화 날짜"). 복붙한 텍스트에도 드롭한 파일과 같은 추출 규칙을 적용합니다.
- 기본 날짜 패턴은 `2026-09-25`, `2026.9.25`, `2026/09/25`, `2026년 9월 25일`입니다. 실제 사내 메신저 export 형식은 **TODO 5**에서 확인한 뒤 `date_patterns`에 추가하면 됩니다.
- `title`과 `url`은 YAML에서 문제가 되지 않도록 큰따옴표로 감쌌습니다(제목에 `:`가 들어갈 수 있음).
- 파일명: 금지문자 치환 외에 제어문자 제거, 연속 공백 정리, **최대 80자**, 끝의 점/공백 제거를 추가했습니다(Windows 경로 규칙).
- 제목 제안 결과는 첫 줄에서 `제목:` 접두어, 마크다운 기호, 감싼 따옴표를 떼고 최대 50자로 자릅니다. CLI가 실패하면 본문 첫 줄(최대 30자)을 씁니다.
- 저장이 끝나면 제목과 본문 입력칸을 비웁니다(같은 대화를 두 번 저장하지 않도록).

## 3. Confluence 저장
- **제목은 가져온 마크다운의 첫 `# 헤딩`**에서 뽑습니다. 헤딩이 없으면 기존 파일의 frontmatter 제목을 쓰고, 그것도 없으면 pageId를 씁니다. 화면의 "제목" 입력칸은 메신저 전용이라 Confluence에는 쓰지 않았습니다.
- pageId를 찾지 못하면 `h` + URL SHA-1 앞 12자리를 키로 씁니다(숫자 pageId와 겹치지 않게).
- CLI 출력 전체가 ```` ```markdown ... ``` ````로 감싸져 있으면 벗겨냅니다(LLM이 흔히 이렇게 출력함).
- 같은 pageId 파일이 여러 개 있으면(정상 상황은 아님) 새 파일 하나만 남기고 나머지는 지웁니다.
- 제목이 바뀐 경우 "새 이름으로 쓴 뒤 옛 파일 삭제"로 rename을 처리합니다. 대기 목록의 옛 경로도 새 경로로 바꿉니다.
- 실패 판정: 종료코드가 0이 아니거나, 출력이 비어 있거나 공백 또는 빈 코드블록뿐일 때입니다. 로그인 페이지 HTML처럼 "내용은 있지만 잘못된" 출력은 가려내지 못합니다. 스킬의 실패 출력 형태를 보고 필요하면 보완하겠습니다(TODO 2).

## 4. 대기 목록 / Wiki 변환
- `.wiki_collector_pending.json` 형식: PLAN의 "경로 배열" 대신 `[{"path": "...", "status": "new"|"updated"}]`로 저장합니다. 확인창에 "신규 N개, 업데이트 M개"를 보여주려면 상태가 필요하기 때문입니다. 경로 문자열만 있는 배열도 읽을 수 있습니다.
- 대기 중인 신규 파일을 다시 업데이트해도 상태는 `new`로 유지합니다(wiki 입장에서는 여전히 신규).
- 하단 표시는 "대기 중인 파일 N개 (신규 a, 업데이트 b)"로 했습니다. 업데이트가 0개면 괄호 부분은 생략합니다.
- 변환 직전에 탐색기에서 이미 지운 파일은 대기 목록에서 자동으로 뺍니다.
- 변환에 성공하면 **변환에 보낸 항목만** 목록에서 지웁니다(변환 중에 새로 추가된 항목은 남깁니다).
- `{files}`는 PLAN대로 raw 기준 상대경로를 `- ` 목록으로 넣고, cwd가 wiki_dir이므로 프롬프트에 `{raw_dir}` 절대경로도 함께 넣었습니다.
- 성공/실패 모두 결과 창(Toplevel)에 출력을 보여줍니다. 줄 맨 앞(목록 기호 허용)에 `미분류:`가 있는 줄은 창 위쪽에 빨간색으로 따로 모아 보여줍니다.
- 기본 `wiki_prompt`의 "논의 이력" 섹션 이름과 형식은 PLAN 문구를 그대로 썼습니다. 섹션이 없으면 **문서 끝에 새로 만들도록** 지시했는데, 이 부분은 제가 정했습니다. **TODO 1**에서 확정해야 합니다.

## 5. UI / 기타
- **`txt 열기…` 버튼을 추가**했습니다. tkinterdnd2가 없거나 드롭이 안 되는 환경에서 대체 수단으로 씁니다. 드롭과 같은 로직을 씁니다.
- 여러 파일을 드롭하면 첫 번째 `.txt`만 불러옵니다. `.txt`가 아니면 무시하고 상태줄에 안내합니다.
- 대기 목록이 비어 있으면 `Wiki로 변환` 버튼을 비활성화합니다.
- Windows에서 DPI awareness를 켰습니다(고해상도 화면에서 글자 번짐 방지).
- `WIKI_COLLECTOR_CONFIG` 환경변수로 config 경로를 바꿀 수 있게 했습니다(테스트/stub 실행용).
- `config.json`은 사용자 경로가 들어가서 `.gitignore`에 넣었습니다. 기본값은 코드(`DEFAULT_CONFIG`)에 있습니다.
- `build.bat` 메시지는 영문으로 썼습니다. 배치 파일의 한글은 코드페이지(cp949/UTF-8) 때문에 깨지거나 파싱 오류가 날 수 있습니다. 빌드 전에 테스트를 먼저 돌리고, `--collect-all tkinterdnd2`로 tkdnd 네이티브 라이브러리를 포함합니다.
- 앱 코드는 PLAN 예상(~300줄)보다 긴 약 730줄입니다(주석, 기본 프롬프트 포함). 로직을 GUI와 분리해 테스트할 수 있게 하느라 늘었습니다. 파일은 PLAN대로 단일 파일입니다.
- PLAN.md를 저장소 루트에 함께 커밋했습니다(이 메모가 참조하는 기준 문서).

## 5-1. 요청 반영 (2차)
- **txt 드롭을 창 전체에서** 받도록 바꿨습니다. root와 모든 하위 위젯을 드롭 대상으로 등록합니다(결과 창은 제외).
- **Confluence URL 입력칸과 `가져오기` 버튼을 없애고 본문창을 함께 씁니다.** `raw에 저장`을 눌렀을 때 본문 전체가 `http(s)://` URL **한 줄뿐이면** Confluence 가져오기로, 아니면 메신저 저장으로 처리합니다. URL이 들어 있는 메신저 대화는 여러 줄이거나 다른 글자가 섞여 있으므로 메신저로 저장됩니다. 도메인으로는 구분하지 않았습니다(사내 Confluence 주소를 몰라서). 가져오기에 실패하면 본문의 URL을 그대로 남겨 두어 바로 재시도할 수 있습니다.

## 6. 검증한 것 / 못 한 것
- ✅ `python test_wiki_collector.py`: 13개 테스트 통과 (pageId, 파일명, 중복 탐색, cp949/utf-8, 대기 목록, 날짜 추출, 템플릿, stub CLI 실행, timeout 시 트리 종료, 빈 출력 방어)
- ✅ Xvfb에서 GUI 시나리오 자동 실행 (stub CLI): 복붙 → 자동 제목 → 저장 / txt(cp949) 로드 → 파일명 제목 / 같은 URL 두 번 → 신규 추가 후 업데이트, 파일 1개 / CLI 실패 → 기존 파일 보존 / 변환 확인창 → 목록 비움
- ❌ 실제 드래그앤드롭 이벤트 (가상 디스플레이라 드롭을 흉내 낼 수 없어서, 드롭 핸들러가 호출하는 `load_txt`만 검증)
- ❌ Windows 실행, exe 빌드, 실제 claude/codex CLI 연동 → PLAN의 TODO 1~5와 함께 사내 PC에서 확인 필요

## 7. PR 리뷰 반영 (sangria-kim/wiki_collector#1)
| 등급 | 지적 | 조치 |
| --- | --- | --- |
| 🔴 | 종료코드 0으로 끝난 에러 설명이 Confluence 본문으로 저장됨 | `confluence_prompt`에 "성공하면 `# 제목`으로 시작, 실패하면 `ERROR: 이유` 한 줄만"을 추가함. 출력이 `ERROR:`로 시작하거나 `# ` 헤딩으로 시작하지 않으면 저장하지 않음(`confluence_require_heading`, 기본 true). 업데이트할 때 기존 파일은 `<파일>.md.bak`으로 1개 보관함 |
| 🔴 | `` ```markdown\n``` ``가 빈 본문으로 판정되지 않음 | 코드펜스 정규식을 리뷰 제안대로 고치고 테스트 케이스를 추가함 |
| 🟡 | Windows 인라인 모드에서 `\"`가 cmd.exe에 통하지 않음 | Windows에서는 값 속 `"`를 `'`로 바꿔 따옴표가 닫히지 않게 함. `%VAR%` 확장은 막지 못하므로 docstring에 "긴 본문은 stdin 방식 권장"을 적어 둠 |
| 🟡 | 문장 중간의 날짜를 대화 날짜로 잡음 | 줄 맨 앞(대괄호 허용)의 날짜를 먼저 찾고, 없을 때만 본문 전체에서 찾음 |
| 🟡 | 도메인과 상관없이 URL이면 Confluence로 처리 | config에 `confluence_host`를 추가함(비어 있으면 기존처럼 모든 URL). 값이 있으면 그 host가 들어간 URL만 Confluence로 처리하고, 나머지는 메신저로 저장함 |
| ⚪ | pending JSON 형식 검증 | 최상위가 list가 아니면 빈 목록으로 처리함. `path`가 없는 항목은 무시하고, `status`가 없으면 `new`로 채움 |
| ⚪ | 첫 실행 때 빈 창이 보임 | 폴더 선택 중에는 `withdraw`, 끝나면 `deiconify` |
| ⚪ | codex 출력의 첫 줄이 배너일 가능성 | **수정하지 않음.** 실제 출력 형태를 모르므로 TODO 3에서 확인한 뒤 결정함(필요하면 마지막 줄 사용으로 변경) |
